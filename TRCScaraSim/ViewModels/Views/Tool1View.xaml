<UserControl x:Class="TRCScaraSim.ViewModels.Views.Tool1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TRCScaraSim.ViewModels.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding TestText}"></TextBox>
            <Button Width="30" Content="测试" Command="{Binding TestButtonCommand}"></Button>
        </StackPanel>   
    </Grid>
</UserControl>
